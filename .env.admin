# Copia este archivo a ".env.admin" (sin ".example") y pon tu clave real ahí.
# ".env.admin" está en .gitignore — nunca se sube al repo.
ADMIN_EMAIL=admin@misionemprende.cl
ADMIN_PASSWORD=MisionAdmin2025!
