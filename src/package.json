{
  "name": "mision-emprende-serverless",
  "version": "0.1.0",
  "private": true,
  "description": "Backend serverless para Mision Emprende",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test",
    "local:table": "sh scripts/create-local-table.sh",
    "local:table:reset": "sh scripts/create-local-table.sh --reset",
    "admin:create": "node scripts/create-admin-user.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1091.0",
    "@aws-sdk/lib-dynamodb": "^3.1091.0"
  }
}
